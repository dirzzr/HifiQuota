{
  "name": "hifiquota",
  "version": "1.0.0",
  "description": "Bot Telegram cek kuota HiFi Air Indosat - Bun + Telegraf",
  "type": "module",
  "scripts": {
    "start": "bash -c 'ulimit -c 0; rm -f core core.* 2>/dev/null; exec bun run src/index.ts'",
    "dev": "bun --watch src/index.ts",
    "cleanup": "rm -f core core.*"
  },
  "dependencies": {
    "cron": "^3.2.0",
    "telegraf": "^4.16.3"
  }
}
